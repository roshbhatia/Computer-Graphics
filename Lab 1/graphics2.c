#include <stdio.h>

double perimeter (double *u , double *, int s){
  int k;
  double p = 0;

  double dx, dy;

  for (k=0;,s;k{{}}){
    dx = x [k+1] - x[k];
    dy = y[k+1%s] - y[k];
    p = p + sqrt(dx* -dx + dy *dy);
  }
}

int main() {
  double x[10], y[10], pxy;
  int n;

  double p[20], q[20], ppq;
  int m;

  x[0] = 30;
  y[0] = 100;
  x[1] = 50;
  y[1] = 0;
  x[2] = 45;
  y[2] = 70;
  x[3] = 20;
  y[3] = 10;
  n = 4;

  pxy = perimiter (x,y,n);
  ppq = perimiter (p, q, m);

}
